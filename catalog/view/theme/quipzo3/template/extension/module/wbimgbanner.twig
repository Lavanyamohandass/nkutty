<div class="container martb">
<div class="row imgbnr rless">
    {% for banner in banners %}
    <div class="col-sm-4 col-xs-12 bann cless">
    	<div class="beffect">
	        {% if banner.link %}
			    <a href="{{ banner.link }}" class="beff_a">
			    <img src="{{ banner.image }}" alt="{{ banner.title }}" class="img-responsive center-block" />
			    </a>
			    {% else %}
			    <img src="{{ banner.image }}" alt="{{ banner.title }}" class="img-responsive center-block" />
		    {% endif %}
		    <div class="imgbnrtext img-text">
				<div>
					{{ banner.description }}
					<a href="{{ banner.link }}" class="btn btn-primary">{{ shop_now }}</a>
				</div>
			</div>
		</div>
	</div>
    {% endfor %}
</div>
</div>