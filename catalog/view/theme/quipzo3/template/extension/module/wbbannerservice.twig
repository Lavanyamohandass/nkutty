<div class="bannerser container">
<div class="row">
    {% for banner in banners %}
    <div class="col-sm-12 col-xs-12">
    	<div class="beffect">
	        {% if banner.link %}
			    <a href="{{ banner.link }}" class="beff_a">
			    <img src="{{ banner.image }}" alt="{{ banner.title }}" class="img-responsive center-block" />
			    </a>
			    {% else %}
			    <img src="{{ banner.image }}" alt="{{ banner.title }}" class="img-responsive center-block" />
		    {% endif %}
		    <div class="imgbnrtext serbnrtext">
				<div>
					{{ banner.description }}
					<a href="{{ banner.link }}" class="btn btn-primary">{{ shop_now }}</a>
				</div>
			</div>
		</div>
	</div>
    {% endfor %}
</div>
</div>